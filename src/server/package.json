{
  "name": "server",
  "version": "1.0.0",
  "description": "workspace for backend server code",
  "license": "MIT",
  "author": "shift",
  "type": "commonjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
