# need fun to be a fun/ so that relative links are correct.  Doing it this way doesn't seem to work though
# /fun /fun/ 301!

# this would work if all of the assets were contained under the /fun path, but they aren't quite

/fun/*  http://shift2bikes.org/fun/:splat 200

# some images still live in old file tree

/cal/* http://shift2bikes.org/cal/:splat 200
